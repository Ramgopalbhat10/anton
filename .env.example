# OpenRouter API key — create one at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# OpenCode Go API key — subscribe at https://opencode.ai/auth
OPENCODE_GO_API_KEY=sk-...

# Default model (provider-qualified id). See https://opencode.ai/docs/go/#endpoints
DEFAULT_MODEL=opencode-go/deepseek-v4-flash

# Absolute path to the directory the agent is allowed to read/write/bash in.
# Defaults to ./workspace (created if missing).
WORKSPACE_ROOT=./workspace

# Local GitHub-backed project workspaces.
ANTON_LOCAL_WORKSPACES_ROOT=
GH_TOKEN=
GITHUB_TOKEN=
GITHUB_PAT=
GITHUB_APP_ID=
GITHUB_APP_SLUG=
GITHUB_APP_CLIENT_ID=
GITHUB_APP_CLIENT_SECRET=
GITHUB_APP_PRIVATE_KEY=
GITHUB_APP_SETUP_URL=
GH_CONFIG_DIR=

# Optional git identity for commits made by Anton inside local/Docker workspaces.
# The Docker entrypoint writes these to global git config user.name/user.email.
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=

# Path to the SQLite database file. Defaults to ./anton.db
DATABASE_URL=./anton.db
